import { describe, it } from "vitest";

// Hits the real thing. Filled in by the ticket that builds the adapter.
describe("claude-code adapter", () => {
  it.todo("talks to the real claude-code");
});
